'use client';
import {useState} from 'react'; import RestaurantShell from '@/components/RestaurantShell'; import {supabase} from '@/lib/supabase';
export default function RestaurantOnboard(){const [form,setForm]=useState({name:'',cuisine:'',description:'',delivery_fee:'39'}),[message,setMessage]=useState('');async function submit(e){e.preventDefault();setMessage('');const {data:{user}}=await supabase.auth.getUser();if(!user){setMessage('Please sign in first.');return}const {error}=await supabase.from('restaurant_applications').insert({applicant_id:user.id,restaurant_name:form.name,cuisine:form.cuisine,description:form.description,delivery_fee:Number(form.delivery_fee)});setMessage(error?error.message:'Application submitted. An admin can review it before the restaurant is created.');if(!error)setForm({name:'',cuisine:'',description:'',delivery_fee:'39'})}return <RestaurantShell title="PARTNER ONBOARDING" subtitle="Register your restaurant"><section className="partner-panel onboarding"><div className="steps"><span className="active">1 <b>Restaurant details</b></span><span>2 <b>Review</b></span><span>3 <b>Approval</b></span></div><div className="panel-head"><div><h2>Tell us about your kitchen</h2><p>Submit your restaurant for review. Once approved, your partner workspace becomes active.</p></div></div><form className="profile-form" onSubmit={submit}><label>Restaurant name<input required value={form.name} onChange={e=>setForm({...form,name:e.target.value})} placeholder="e.g. Royal Tadka Kitchen"/></label><label>Cuisine<input required value={form.cuisine} onChange={e=>setForm({...form,cuisine:e.target.value})} placeholder="e.g. Indian, Chinese"/></label><label>Delivery fee<input required type="number" min="0" step="0.01" value={form.delivery_fee} onChange={e=>setForm({...form,delivery_fee:e.target.value})}/></label><label>Description<textarea rows="5" value={form.description} onChange={e=>setForm({...form,description:e.target.value})} placeholder="Tell customers what makes your kitchen special"/></label><div><button className="partner-btn primary"><span className="material-symbols-outlined">send</span>Submit Application</button></div></form>{message&&<div className="partner-alert"><span className="material-symbols-outlined">info</span>{message}</div>}</section></RestaurantShell>}
